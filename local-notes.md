For deploying to preview and live channels:

https://firebase.google.com/docs/hosting/test-preview-deploy

To update my javascript:

`npm run build`

To run emulators??

I think if I do `cd ..` and then run `firebase init emulators`, it runs the emulators for everything. But I'm not sure exactly how it all works, or how to load the UI.

I guess I need to run `firebase emulators:start` now, but it says I need Java??